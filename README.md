# JLC2KiCad Tauri

立创商城(LCSC)/EasyEDA 元器件转 KiCad 库桌面工具，基于 Tauri 2 开发。

## 简介

JLC2KiCad 是一个桌面应用程序，用于从立创商城(EasyEDA/LCSC)搜索电子元器件，并生成 KiCad 格式的元件库。支持：

- 🔍 EasyEDA/LCSC 搜索
- 📦 元器件封装转换
- 🖼️ 实时预览元件图片
- 💾 导出 KiCad 符号库和封装库

## 技术栈

- **前端**: Vanilla JavaScript + Vite
- **后端**: Rust + Tauri 2
- **HTTP 客户端**: reqwest
- **UI**: 自定义 CSS

## 开发进度

### ✅ 已完成
- [x] Tauri 2 项目基础架构
- [x] EasyEDA/LCSC API 集成
- [x] 搜索结果展示（支持图片预览）
- [x] 本地图标系统（按封装类型分类）
- [x] 导出功能基础框架
- [x] 代理设置支持

### 📋 待完成
- [ ] KiCad 符号库生成
- [ ] KiCad 封装库生成  
- [ ] 3D 模型支持
- [ ] 历史记录
- [ ] 批量导出

## 全平台编译教程

> Tauri 2 安装和环境配置请参考官方文档：https://v2.tauri.app/start/prerequisites/

### 前置要求

1. **Node.js** (>= 18)
2. **Rust** (>= 1.70)
3. **Cargo** (Rust 包管理器)

### 安装依赖

```bash
# 安装 Node.js 依赖
npm install

# 安装 Rust 依赖（自动通过 Cargo.toml）
cargo fetch
```

### 开发模式

```bash
# 前端开发服务器
npm run dev

# Tauri 开发模式（带热重载）
cargo tauri dev

# Linux 稳定模式（解决 WebKitGTK 兼容问题）
npm run tauri:dev:stable
```

### 生产构建

```bash
# 构建前端
npm run build

# 构建 Tauri 应用
cargo tauri build
```

构建产物位于：
- 前端: `dist/`
- 安装包: `src-tauri/target/release/bundle/`

## 常见问题

```
jlc2kicad-tauri/
├── src/                    # 前端源码
│   ├── index.html         # HTML 入口
│   ├── main.js            # JavaScript 逻辑
│   ├── style.css          # 样式文件
│   └── assets/icons/      # 本地图标
├── public/                # 静态资源（构建时复制到 dist）
├── src-tauri/             # Rust 后端
│   ├── src/
│   │   ├── main.rs        # 主入口
│   │   ├── lib.rs         # 库代码
│   │   ├── network.rs     # 网络请求
│   │   ├── types.rs       # 类型定义
│   │   └── helpers.rs     # 辅助函数
│   └── Cargo.toml         # Rust 依赖
├── vite.config.js         # Vite 配置
└── package.json            # NPM 配置
```

## 配置代理

应用内置代理设置，支持分别为 EasyEDA 和 LCSC 配置不同代理：

1. 点击「网络设置」按钮
2. 勾选需要使用代理的服务
3. 输入代理地址（如 `http://127.0.0.1:10808`）
4. 点击保存

## 常见问题

### Linux 下窗口显示空白

```bash
# 使用软件渲染模式
WEBKIT_DISABLE_COMPOSITING_MODE=1 cargo tauri dev
```

### Windows 下 WebView2 未安装

从 [Microsoft](https://developer.microsoft.com/zh-cn/microsoft-edge/webview2/) 下载安装 WebView2 Runtime。

### 编译失败

确保 Rust 和 Cargo 版本是最新的：
```bash
rustup update
cargo update
```

## 打赏

如果这个项目对你有帮助，可以扫码打赏支持一下！

![打赏](src/assets/icons/image.png)

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
